# CLAUDE.md — 戦国七雄・都市開発シミュレーション プロジェクトルール

このフォルダ（`sengoku-city/`）はモノレポ内の独立プロジェクトです。作業はこのフォルダ内で完結させ、他プロジェクトには触れません。

## 1. プロジェクトの目的
- ブラウザで動く、中国・戦国時代後期（前247年開始）を舞台にした3D都市開発シミュレーション。
- 最重要方針: ①時代考証にできるだけ忠実 ②長く遊べる奥行き ③都市開発が常に主役。
- 詳細仕様は `docs/01_ゲーム設計書.md`。設計と実装が食い違ったら設計書を更新する。

## 2. 技術方針
- 言語: JavaScript（ES Modules）+ JSDoc型注釈。ビルド工程なし。`index.html` を静的ホスティングに置くだけで動く。
- 3D: Three.js r160 を `vendor/three/` に同梱（CDN 依存なし）。更新は `npm run vendor:three`（要ネット接続）で行い、CREDITS.md のバージョンも更新する。
- テスト: Node 標準の `node --test`（外部依存なし）。`npm test` で実行。
- ロジック（`src/sim/`）は DOM / Three.js を一切 import しない。描画（`src/render/`）と UI（`src/ui/`）はシミュレーション状態を読むだけで、書き換えは必ずコマンド経由。
- データ駆動: 建物・区画・人物・技術・イベント・交易品・国・都市・実績などはすべて `data/*.json`。コードにマジックナンバーや固有名詞をハードコードしない。バランス定数は `data/balance.json`。
- 乱数は必ずシード付きPRNG（`src/core/rng.js`）を使う。`Math.random` 禁止（テストの再現性のため）。
- パフォーマンス: 同種オブジェクトは InstancedMesh、遠景は LOD、住民表示は上限あり（人口の数値と表示人数は別）。
- UI 文言はすべて日本語。時代用語は `data/terms.json` に登録し、初出時にツールチップを出す。

## 3. 3Dモデル
- まず全オブジェクトをコード生成のローポリモデル（`src/render/models/`）で実装する。
- モデルは `data/assets.json`（アセット定義表）経由で参照し、glTF/GLB が用意できたものから差し替える。
- 同じ区画・同じレベルでも最低3種の外観バリエーションを持たせる。
- 縮尺: 1マス ≈ 4m。人の身長 ≈ 0.45（`PERSON_SCALE`）、戸口は 0.42 以上、牛の背 ≈ 0.28。新しいモデルはこの縮尺に合わせる。
- 質感: `builder.js` は頂点ごとに材質（木・土壁・瓦・茅・葉・草・布・石）を記録し、`materials.js` の手続きテクスチャで模様を付ける。ベタ塗りにしない。
- 動くもの（人・牛馬・車・舟）のモデルは前が +z。向き・車輪・脚の動きは `vehicles-view.js` / `people-view.js` が付ける。
- 表示モード（データマップ）はゲームの状態を読むだけ（`sim/view-data.js` が値、`render/view-mode.js` が描画）。セーブには含めない。新しい状態値を足したら `STATE_MODES` に項目を追加する。
- 技術・官職の効果は `sim/modifiers.js` で1つの補正（`world.mods`）にまとめ、各処理はそれを読む。新しい効果種別は `applyEffect` と `research.js` の `effectSummary` に追加する。人物データは `data/persons.json`（出典と `note` 必須、能力は史書の評価による相対値）。
- 案内役の助言は `data/advice.json`（条件・文・優先度・間隔）。文はコードに書かない。案内役は創作のキャラクター（`advice.json` の `characters`: フクロウ「ホウ」`assets/ui/advisor.png`、里の役人「カンコ」`assets/ui/advisor-kanko-*.png`（状態ごとに4枚）、設定で選ぶ）で、史実・漫画・映画・実在の人物の画像や設定は使わない（ユーザーから渡されても断る）。口癖は `tone.endings`（文末の言い換え）と `overrides`（id ごとの文の差し替え）で、文をコードに書かない。表情差分は持たず、動きと記号で状態を表す。
- 外部素材は CC0 など再配布可能なものだけ。出典とライセンスを `CREDITS.md` に必ず記録する。

## 4. 時代考証ルール（違反は不具合として扱う）
- 登場禁止: 紙、鐙、椅子・テーブルの生活、仏教・寺院、綿、茶、新大陸作物（トウモロコシ・唐辛子・サツマイモ等）、火薬、後世の建築様式（明清風楼閣など）、一輪車、アーチ石橋、磁器。
- 建物は木造・版築・瓦葺き/茅葺き。服装・乗り物・武器も戦国時代のものに限る。
- 官職名・制度名・地名は当時のものを使う。迷う点・諸説ある点は `docs/02_時代考証メモ.md` に根拠付きで記録し、オーナーに確認する。
- ゲーム性のために簡略化した点も同メモに明記する。
- 登場人物は史実の人物のみ。生没年・出身・事績は史書に基づき、確証のない情報は `"note": "要確認"` を付ける。
- 漫画『キングダム』独自のキャラクター・設定・台詞・デザインは一切使用しない。

## 5. 開発の進め方
- `docs/05_フェーズ計画.md` のフェーズ順に進める。フェーズ完了時は必ず「実際に動く状態」にする。
- 各フェーズ完了時に報告する内容: 実装内容 / 動作確認の手順（操作レベルで具体的に、日本語）/ 既知の課題。オーナーの確認を待ってから次フェーズへ進む。
- フェーズごとに git コミットする。設計から変更した点は設計書に反映する。
- ロジック部分には自動テストを書く。`npm test` が通らない状態でコミットしない。
- 不明点は推測で進めず質問する。ただし質問は設計に影響する点に絞り、細部は妥当な既定値で進めて報告に明記する。
- 説明はプログラミング非専門家向けに、平易な日本語で簡潔に。

## 6. コーディング規約
- ファイル名: kebab-case（`zone-growth.js`）。ID: snake_case の ASCII（`house_commoner`）。表示名は日本語で JSON の `name` に持つ。
- 1ファイル1責務。300行を超えたら分割を検討。
- コメントは「なぜ」を書く。史実の根拠は該当箇所にコメントで出典を添える。
- コミットメッセージは日本語で「何を・なぜ」。フェーズ番号を先頭に付ける（例: `P1: 地形生成と道路配置`）。

## 7. コマンド
```
npm test              # ロジックの自動テスト
npm run serve         # ローカル確認用サーバー（http://localhost:8080/）
npm run validate      # data/*.json の整合性チェック
npm run vendor:three  # Three.js を vendor/ に同梱（要ネット接続）
```

## 8. 描画の確認について
- `tools/touch-check.mjs` でタッチ端末（iPad 相当）とマウスの操作を自動確認できる。入力やレイアウトを変えたら実行する。
- `tools/screenshot.mjs`（Playwright + headless Chromium）で実際の描画のスクリーンショットを撮れる。描画を変えたら必ず撮って目視で確認し、報告に改善前後の画像を添える。
- 描画は `src/render/` に閉じる。見た目の改善でロジック（`src/sim/`）を変えてはならない。
- 面の向き（表裏）に注意: builder の `triOut`/`quadN` は外向き・法線向きに自動で揃える。新しい形状はスクリーンショットで裏面が消えていないか確認する。
