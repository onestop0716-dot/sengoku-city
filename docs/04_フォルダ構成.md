# 04 フォルダ構成

```
sengoku-city/
├── index.html                # 唯一のページ。ES Modules で src/main.js を読む
├── package.json              # scripts のみ（test / serve / validate）。実行時依存なし
├── CLAUDE.md  README.md  CREDITS.md
├── docs/                     # 設計書・考証メモ（本フォルダ）
├── data/                     # ゲーム内容の JSON（03_データ定義設計.md 参照）
│   ├── nations.json  cities.json  terrain.json  zones.json  buildings.json
│   ├── crops.json  structures.json  goods.json  persons.json  offices.json
│   ├── ranks.json  techs.json  events.json  assets.json  terms.json
│   └── achievements.json  difficulties.json  balance.json  advice.json（案内役の助言）
├── assets/
│   ├── ui/                   # UI 用の画像（advisor.png: 案内役のフクロウ）
│   └── models/               # 外部 glTF/GLB（導入時のみ。CREDITS.md に記録）
├── vendor/
│   └── three/                # three.module.js と addons（OrbitControls, GLTFLoader）
├── src/
│   ├── main.js               # 起動: データ読込 → World 生成 → Render/UI 接続 → ループ
│   ├── core/                 # 依存なしの汎用部品
│   │   ├── rng.js            # シード付き乱数（mulberry32）
│   │   ├── event-bus.js
│   │   ├── grid.js           # 2D 配列・近傍・矩形ユーティリティ
│   │   └── pathfinding.js    # 道路グラフの経路探索（住民・乗り物・商隊）
│   ├── data/
│   │   ├── registry.js       # JSON 読込・検証・ID 参照
│   │   └── validate.js       # 検証ルール（validate スクリプトと共有）
│   ├── sim/                  # ★純ロジック。DOM/Three.js を import しない
│   │   ├── world.js          # World の生成・tick の統括
│   │   ├── commands.js       # UI からのコマンドの検証と適用
│   │   ├── calendar.js       # 年月季節
│   │   ├── terrain/          # 地形生成（ハイトマップ、川、資源）
│   │   ├── roads.js          # 道路配置と接続判定
│   │   ├── zones.js          # 区画の設定・繁栄度・成長・衰退
│   │   ├── placement.js      # 建物の向き（道路側 / 通りの長さ / 南向き）
│   │   ├── structures.js     # 特殊建築の配置・効果範囲・維持費
│   │   ├── economy.js        # 税・収支・価格
│   │   ├── population.js     # 人口・身分・民忠・治安・衛生
│   │   ├── demand.js         # 需要メーター
│   │   ├── advisor.js        # 案内役: 指標の集計と助言の選択（純ロジック、テスト対象）
│   │   ├── view-data.js      # 表示モード（データマップ）のマスごとの値・区画の集計・配置の改善見込み（純ロジック）
│   │   ├── agents.js         # 表示用住民・乗り物の「論理」位置（描画とは別）
│   │   ├── persons.js        # 人材・官職・登用
│   │   ├── research.js       # 技術・制度
│   │   ├── nation/           # 全国マップ: 国力モデル、外交、AI、出世
│   │   ├── trade.js          # 交易品・商隊
│   │   ├── military/         # 徴兵・鍛錬・軍・battle.js（戦闘判定）
│   │   ├── events.js         # ランダム／史実イベント
│   │   ├── achievements.js
│   │   └── save/             # 直列化・スロット・移行
│   ├── render/               # Three.js 描画。World を読むだけ
│   │   ├── scene.js  camera.js  lights.js
│   │   ├── terrain-field.js  # 連続した高さ場と頂点色（純粋な計算、テスト対象）
│   │   ├── terrain-mesh.js   # 地形メッシュ・区画オーバーレイ・水面シェーダー
│   │   ├── buildings.js      # InstancedMesh 管理、バリエーション、LOD
│   │   ├── agents-view.js    # 住民・乗り物の移動（経路・向きの補間・坂の傾き）と近景/遠景の振り分け
│   │   ├── people-view.js    # 関節つきの住民（部品ごとの InstancedMesh、歩行・作業）
│   │   ├── vehicles-view.js  # 乗り物の組み立て（車体・回る車輪・牛馬の脚・舟）
│   │   ├── crop-stage.js     # 畑の季節段階（暦 → 芽/成長/実り/刈り株）
│   │   ├── view-mode.js      # 表示モードの描画: マスの色を W×H テクスチャにまとめ地形に重ねる。効果範囲の輪、配置プレビュー
│   │   ├── materials.js      # 手続きテクスチャ（木目・土壁・瓦・茅・草）のマテリアル
│   │   ├── overlay.js        # 区画の色、選択枠、効果範囲
│   │   ├── picking.js        # クリック→マス／建物
│   │   ├── models/           # コード生成ローポリモデル（建物種別ごと）
│   │   ├── assets/           # assets.json の解決、glTF ローダー、フォールバック
│   │   └── battle-view.js    # 戦闘観戦
│   ├── ui/                   # DOM（HTML + CSS）。日本語
│   │   ├── hud.js  toolbar.js  settings-panel.js  panels/（財政・人口・人材・研究・軍・外交・貿易・実績）
│   │   ├── national-map.js   # 全国マップ（Canvas 2D）
│   │   ├── tooltip.js        # terms.json 連動
│   │   ├── advisor.js        # 案内役（フクロウの画像、動きと記号で状態表現、吹き出し、やるべきこと・履歴、最小化）
│   │   ├── view-mode-panel.js # 表示モードのメニュー・凡例・ホバーの内訳・キー操作（Tab / Esc）
│   │   └── styles.css
│   └── app/
│       ├── game-loop.js      # requestAnimationFrame と固定ステップ tick の橋渡し、速度
│       ├── input.js          # 左ドラッグでツール適用（区画・道路・撤去・選択）
│       └── settings.js       # 表示設定（描画品質 高/中/低）
├── tests/                    # node --test（sim / core / data の検証）
└── tools/
    ├── validate-data.mjs     # npm run validate
    ├── serve.mjs             # npm run serve（依存なしの静的サーバー）
    ├── vendor-three.mjs      # npm run vendor:three（Three.js の同梱・更新）
    └── screenshot.mjs        # 実描画のスクリーンショット（Playwright が必要）
```

## 依存関係の向き（守ること）
```
core ← sim ← app → render
        ↑          ↑
       data       ui → sim(commands のみ)
```
- `sim` は `core` と `data` だけに依存する。
- `render` と `ui` は `sim` の状態を読むが、書き換えは `commands` 経由。
- `app` が全体を組み立てる。

## GitHub Pages
- リポジトリ直下の `.github/workflows/sengoku-city-pages.yml` で `sengoku-city/` フォルダをそのまま Pages に公開する（`paths: sengoku-city/**` で他プロジェクトの変更では動かない）。
- ビルドなしなので、ローカルで動くものがそのまま公開される。
